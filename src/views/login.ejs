<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Вход — TaskTracker</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<main class="auth-page">
  <% if (currentUser) { %>
    <div class="auth-form">
      <h1>TaskTracker</h1>
      <p>Вы уже вошли как <strong><%= currentUser.display_name %></strong> (@<%= currentUser.username %>).</p>
      <a href="/" class="button">На дашборд</a>
      <form method="post" action="/logout">
        <button type="submit" class="ghost-btn" style="width: 100%">Выйти и войти другим аккаунтом</button>
      </form>
    </div>
  <% } else { %>
  <form class="auth-form" method="post" action="/login">
    <h1>TaskTracker</h1>
    <% if (error) { %><p class="error"><%= error %></p><% } %>
    <label>Логин<input type="text" name="username" required autofocus></label>
    <label>Пароль<input type="password" name="password" required></label>
    <button type="submit">Войти</button>
    <p class="muted">Нет аккаунта? <a href="/register">Зарегистрироваться</a></p>
  </form>
  <% } %>
</main>
</body>
</html>
