# Generate a real secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-me

# Set to true once served over HTTPS (behind Caddy/nginx with TLS); false is fine over plain HTTP.
COOKIE_SECURE=false
